import TabsComponentDocumentation from "solid-a11y?component-docs=tabs/index.tsx:TabGroup,Tabs,Tab,TabPanel";
import { COMPONENTS } from "@docs/components";
import {
  AriaSpecLink,
  CodeSampleShowcase,
  ComponentAPIExplorer,
  RunningShowcase,
} from "@docs/showcase";

import TabsBasicExample from "./basic.example";
import ProcessedBasicExample from "./basic.example?prism";
import ProcessedConditionalExample from "./conditional.example?prism";
import ProcessedControlledExample from "./controlled.example?prism";
import ProcessedDisabledTabExample from "./disabled.example?prism";
import ProcessedInitialIndexExample from "./initial-index.example?prism";
import ProcessedWithLabelExample from "./with-label.example?prism";
import ProcessedVerticalExample from "./vertical.example?prism";

# {COMPONENTS.tabs.title}

These components constitute a "tabbed" interface that supports keyboard interactions and focus
management.

<RunningShowcase source={ProcessedBasicExample} class={COMPONENTS.tabs.color}>
  <TabsBasicExample />
</RunningShowcase>

## Labeling

One can label the group by using the standard `<Label />` component.

<CodeSampleShowcase source={ProcessedWithLabelExample} />

## Controlling the Current Tab

By default, the tab components control their own state, and the first tab is initialized as visible.
One may pass `initialIndex` to override this behavior.

<CodeSampleShowcase source={ProcessedInitialIndexExample} />

It is also possible to fully "control" the tab components by using a `selectedIndex` and `onChange`
pair.

<CodeSampleShowcase source={ProcessedControlledExample} />

## Conditional Content and Classes

`<Tab />` supports optional render callback with a `selected` signal getter on both the `children`
and `classList` props to support conditional classes and tab content.

Moreover, `<TabPanel />` supports an optional callback for `children` (like `<Show />`) for lazy
and/or expensive children.

<CodeSampleShowcase source={ProcessedConditionalExample} />

## Disabled Tabs

A tab's pointer and keyboard interactions may be prevented with the `disabled` prop.

<CodeSampleShowcase source={ProcessedDisabledTabExample} />

## Vertical Tabs

Where tabs are visually oriented vertically (i.e., from top-to-bottom, rather than left-to-right),
setting the `orientation` prop as `"vertical"` inverts the keybindings to use up and down arrows
instead.

<CodeSampleShowcase source={ProcessedVerticalExample} />

<ComponentAPIExplorer components={TabsComponentDocumentation} />

<AriaSpecLink href="tabpanel" />
