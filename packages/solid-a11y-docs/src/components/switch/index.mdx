import SwitchComponentDocumentation from "solid-a11y?component-docs=switch/index.tsx:Switch";

import { COMPONENTS } from "@docs/components";
import {
  AriaSpecLink,
  CodeSampleShowcase,
  ComponentAPIExplorer,
  RunningShowcase,
} from "@docs/showcase";

import SwitchBasicExample from "./basic.example";
import ProcessedBasicExample from "./basic.example?prism";
import ProcessedWithDirectLabelExample from "./with-direct-label.example?prism";
import ProcessedWithCustomLabelExample from "./with-custom-label.example?prism";

# {COMPONENTS.switch.title}

Switches are an interface for toggling a value between two states and offer the same semantics and
keyboard navigation as native checkbox elements.

<RunningShowcase source={ProcessedBasicExample} class={COMPONENTS.switch.color}>
  <SwitchBasicExample />
</RunningShowcase>

## Proper Labeling

One should label their switch with screen reader friendly text. The easiest way is to add [visually
hidden text][visually_hidden] as a direct child of the switch.

<CodeSampleShowcase source={ProcessedWithDirectLabelExample} />

One can use all the `<Label* />` and/or `<Description* />` components to also provideadditional ARIA
support to the switch, while keeping the text visible and elsewhere in the DOM.

<CodeSampleShowcase source={ProcessedWithCustomLabelExample} />

<ComponentAPIExplorer components={SwitchComponentDocumentation} />

<AriaSpecLink href="switch" />

[visually_hidden]: /labeling-and-descriptions#visually-hidden-elements
